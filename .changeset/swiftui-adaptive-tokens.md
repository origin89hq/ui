---
"@origin89/ui-react": minor
---

Swift: add brand colours that follow light and dark mode (`.origin89.muted`, `Color.origin89.surface`) and `Font.origin89Label`, `.origin89Value` and `.origin89Data`. `Origin89Fonts.register()` no longer requires the main actor. The React package is unchanged; this release carries the Swift tag.
