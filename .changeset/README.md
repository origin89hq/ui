# Changesets

Run `pnpm changeset` when a change should reach package consumers. Choose the affected package and bump type, then describe the result for its users. Include the generated Markdown file in the same PR as the change.

See [the release guide](../docs/releases.md).
